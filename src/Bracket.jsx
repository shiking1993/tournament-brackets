// // import React from 'react';
// import SingleElimination from './SingleElimination';
// import DoubleElimination from './DoubleElimination';

// const Bracket = ({ matches }) => {
//   const isDoubleElimination = matches.lower && matches.lower.length > 0;

//   return (
//     <div className="bracket">
//       {isDoubleElimination ? (
//         <DoubleElimination matches={matches} />
//       ) : (
//         <SingleElimination matches={matches} />
//       )}
//     </div>
//   );
// };

// export default Bracket;
